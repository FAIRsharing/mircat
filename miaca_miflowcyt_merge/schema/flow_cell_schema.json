{
  "id": "https://w3id.org/mircat/miaca_miflowcyt_merge/schema/flow_cell_schema.json",
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "MIFlowCyt Flow Cell",
  "description": "The flow cell of the instrument",
  "type": "object",
  "properties": {
    "@context": {
      "description": "The JSON-LD context",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "object"
        },
        {
          "type": "array"
        }
      ]
    },
    "@id": {
      "description": "The JSON-LD identifier",
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "description": "The JSON-LD type",
      "type": "string",
      "format": "uri"
    },
    "flowCellType": {
      "description": "The flow cell type (e.g., stream-in-air, cuvette) including the material shall be provided.",
      "type": "string"
    },
    "other": {
      "description": "Other relevant flow cell details should be provided and may include flow cell manufacturer name (e.g., MIFlowCyt Optics, Inc.) and model number (e.g., PRO-CFC-75), flow cell outside shape (e.g., rectangular with an attached sphere, spherical, square) and dimensions, flow channel shape (e.g., rectangular, circular, triangular) and dimensions, sheath flow velocity (e.g., 10 l/s), sheath flow pump type (e.g., gas pressure, vacuum, gear), sample flow velocity (e.g., 7.5 l/s) and sample flow pump (e.g., gas pressure, syringe).",
      "type": "string"
    }
  }
}
